#git-exercise
#this project is created based on python3

##start (setup)
1. cloning repository `git clone <uri>`
2. create virtual evironment `python3 -m venv venv`
3. setup the venv `source venv/bin/activate`
4. install librarly `pip install -r requirments.txt`

# git-exercise
